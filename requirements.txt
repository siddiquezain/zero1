pandas>=2.0
pyarrow>=14.0
requests>=2.31
scikit-learn>=1.3
joblib>=1.3
numpy>=1.24
streamlit>=1.40
pydeck>=0.9
plotly>=5.20
python-dotenv>=1.0
scipy>=1.11

# Optional — enables the Claude runtime for the Fire Intelligence Agent.
# The app + deterministic agent work fully WITHOUT this / an API key.
anthropic>=0.40

# Offline-only — used by src/ingestion/visualise.py (sanity-check plots), not by
# the dashboard, the intelligence layer, or the test suite. Not installed on the
# hosted deployment. Add back locally if you run that script:
#   matplotlib>=3.7
#   tqdm>=4.66
